general {
    lock_cmd = pidof hyprlock || hyprlock       # command to run to lock
    before_sleep_cmd = loginctl lock-session    # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on  # turn on display after wake
}


